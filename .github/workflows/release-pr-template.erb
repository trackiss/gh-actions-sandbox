Release <%= Time.now.strftime("%Y-%m-%d") %><%= if ENV['RELEASE_PR_TITLE_SUFFIX'] == "" then "" else " #{ENV['RELEASE_PR_TITLE_SUFFIX']}" %>
<% merged_prs.each do |pr| -%>
<%= "- ##{pr.number}" %>
<% end -%>
