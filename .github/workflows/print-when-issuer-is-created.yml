name: Print when an issuer is created
on:
  issues:
    types: [opened]

jobs: 
  print:
    runs-on: ubuntu-20.04
    steps:
      - run: |
          echo "${{ github.event.issue.title }} ${{ github.event.issue.id }} ${{ github.event.issue.url }}"
